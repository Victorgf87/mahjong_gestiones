apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-rails-app.fullname" . }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
  selector:
    app: {{ include "my-rails-app.name" . }}
